<span class="beginner">Principiante</span>

***

# ¿Qué es una plabra clave/reservada o keyword?

Una **keyword** (o palabra clave) en Java es una palabra reservada que tiene un significado predefinido para el compilador. No se pueden usar como nombres para variables, métodos, clases u otros identificadores, ya que tienen una función específica en la sintaxis del lenguaje.

De normal un editor de código qué este configurado o un IDE podra detectar estas palabras reservadas y las resaltara cambiando la fuente de la letra o solo su color.

Por ejemplo, `public`, `class`, `void`, `static`, `int` y `for` son todas palabras clave. El compilador las reconoce y sabe qué acción o estructura de control representan.

### Ejemplos comunes de Keywords 🏷️

* **Tipos de datos:** `int`, `boolean`, `char`, `double`, etc.
* **Modificadores de acceso:** `public`, `private`, `protected`.
* **Estructuras de control:** `if`, `else`, `switch`, `while`, `for`, `break`, `continue`.
* **Programación orientada a objetos (POO):** `class`, `new`, `this`, `super`, `extends`, `implements`.
* **Manejo de excepciones:** `try`, `catch`, `finally`, `throw`, `throws`.

Entender las palabras clave es crucial, ya que son los bloques de construcción que dan forma a la lógica de cualquier programa Java.